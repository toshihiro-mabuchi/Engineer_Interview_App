<div class="container main top-main">

  <div class="align-top"><%= render 'shared/flash_messages' %></div>

  <div class="row justify-content-center">
    <%# <div class="top-message" style="text-align: center;">
      <h1>面談練習アプリ</h1>
    </div> %>

    <div style="text-align: center;">
      <h2>面談動画パターン選択</h2><br>
      <% @patterns.each do |pattern| %>
        <div class="btn-group">
          <button type="button" class="btn-solid-silver">
            <%= link_to "#{pattern.name}", selected_patterns_path(id: pattern.id), method: :get, class: "btn btn-block w-100" %>
          </button>
        </div><br>
      <% end %>
    </div>
  </div>
  
</div>
