<div class="modal-dialog modal-lg modal-dialog-center;">
  <div class="modal-content">
    <div class="modal-header movie__modal-header">
      <div class="col-10 offset-1">
        <h3 class="modal-title">面談動画新規登録</h3>
      </div>
      <button type="button" class="btn-close" aria-label="close" data-bs-dismiss="modal"></button>
    </div>

    <div class="modal-body movie__modal-body">
      <div class="col-10 offset-1">
        <div class="conteiner">
          <div class="movies__form">
            <%= form_with model: @movie, remote: true do |f| %>
              <div class="movies__input">
                <%= f.label :youtube_url, "YouTubeのURL"%><br>
                <%= f.text_field :youtube_url, placeholder: 'URLを入力してください。', id: "modal-new-foucus", class: "form-control", required: true %><br>

                <%= f.label :text, "コメント" %><br>
                <%= f.text_area :text, placeholder: '動画説明やコメントを入力できます。（300文字以内）',
                                          size: "20x7", class: "form-control memo" %>
              </div>
              <br>
              <%= f.submit class: "btn btn-primary", data: { disable_with: '送信中' } %>
              <button type="button" class="btn btn-outline-dark bg-light btn-edit" data-bs-dismiss="modal">
                キャンセル
              </button>
            <% end %>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>
